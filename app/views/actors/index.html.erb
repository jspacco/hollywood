<%= link_to 'Add a new actor', new_actor_path %>
<table>
   <tr>
       <th>ID</th>
       <th>name</th>
   </tr> 
   
   <% @actors.each do |actor| %>
   <tr>
      <td> <%= actor.id %> </td>
      <td> <%= actor.name %> </td>
   </tr>
   <% end %>
</table>