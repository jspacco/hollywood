<%# new_movie_path is a special function added
by Rails because we told Rails to create all resources
for movie in config/routes.rb.

Check the README file with this project for more information about
these automatically created path function.

%>
<%= link_to 'Add a new movie', new_movie_path %>

<h2>Movies</h2>

<table>
    <% @movies.each do |movie| %>
        <tr>
            <td> <%= movie.id %> </td>
            <td> <%= movie.title %> </td>
            <td> <%= movie.year %> </td>
        </tr>
    <% end %>
</table>
